interface:
  display_name: "微信群记录总结"
  short_description: "读取本人本地微信群记录并生成可核对的网页与长图报告"
  default_prompt: "Use $wechat-group-digest to read my authorized local WeChat group records and create an auditable Chinese HTML, PNG, Markdown, and JSON summary."
policy:
  allow_implicit_invocation: true